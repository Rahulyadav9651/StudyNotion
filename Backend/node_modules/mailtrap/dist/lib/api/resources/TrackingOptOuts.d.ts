import { AxiosInstance } from "axios";
import { CreateTrackingOptOutParams, CreateTrackingOptOutResponse, ListTrackingOptOutsParams, ListTrackingOptOutsResponse, TrackingOptOut } from "../../../types/api/tracking-opt-outs";
export default class TrackingOptOutsApi {
    private client;
    private trackingOptOutsURL;
    constructor(client: AxiosInstance);
    /**
     * List email addresses that have opted out of open and click tracking.
     * The endpoint returns up to 1000 records per request; pass the previous
     * response's `last_id` to fetch the next page.
     */
    getList(params?: ListTrackingOptOutsParams): Promise<ListTrackingOptOutsResponse>;
    /**
     * Add an email address to the tracking opt-out list for a sending domain.
     */
    create(params: CreateTrackingOptOutParams): Promise<CreateTrackingOptOutResponse>;
    /**
     * Remove an email address from the tracking opt-out list so open and click
     * tracking can apply again.
     */
    delete(id: string): Promise<TrackingOptOut>;
}
//# sourceMappingURL=TrackingOptOuts.d.ts.map